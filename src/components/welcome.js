import React from 'react';

export default () => 
	<div className="container">
		<div className="row col-sm-6">
			Select 'Log In' if you are already a registerd user or 'Sign Up' if not.
		</div>
	</div>;
